export const selectDayWaterConsumed = (state) => state.water.dayWaterConsumed;

export const selectIsError = (state) => state.water.isError;

export const selectIsLoading = (state) => state.water.isLoading;
